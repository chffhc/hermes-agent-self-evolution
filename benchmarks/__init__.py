"""Benchmark harnesses owned by the self-evolution repo.

``run_bench.py`` is the legacy smoke/proxy runner used by BenchmarkGate as a
last-resort fallback; it is deliberately import-free and invoked by path.
``capability/`` is the Level 1 native capability benchmark foundation.
"""
